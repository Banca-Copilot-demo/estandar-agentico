{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ejemplo.dev/track-agentico/schemas/2.0/mcp.schema.json",
  "title": "MCP — configuracion de un servidor que EXPONE HERRAMIENTAS al cliente",
  "description": "El unico tipo que anade capacidades ejecutables desde fuera de la organizacion, y el unico que CADUCA SOLO: su aprobacion llega a su `review_by`, o el proveedor cambia las herramientas sin avisar. Un `.mcp.json` es un archivo de configuracion y no admite frontmatter, asi que TODA su metadata vive en el METADATA.json hermano. Esta extension es aportacion propia casi por completo: no encontramos ninguna convencion previa que gobierne MCP. Donde se pudo, se toma el vocabulario del registro oficial (`packages[].registryType/identifier/version` y `remotes[].url`) para que un dia sea interoperable.",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "required": [
    "mcp_servers",
    "credentials",
    "approval"
  ],
  "properties": {
    "name": {
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "El gate exige que el ultimo segmento del `id` coincida con la carpeta que contiene el `.mcp.json`."
    },
    "mcp_servers": {
      "type": "array",
      "minItems": 1,
      "x-read-from": "METADATA.json",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "source",
          "transport"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "source": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "kind",
              "ref",
              "version_pin"
            ],
            "description": "De donde sale el servidor. Equivale a `packages[]` del registro oficial: `kind` es su `registryType`, `ref` su `identifier`, `version_pin` su `version`.",
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "npm",
                  "pypi",
                  "oci",
                  "git",
                  "remote"
                ]
              },
              "ref": {
                "type": "string"
              },
              "version_pin": {
                "type": "string",
                "not": {
                  "const": "latest"
                },
                "description": "Version FIJADA. `latest` esta prohibido y el registro oficial tambien lo rechaza: sin fijar, el contenido que se aprobo y el que se ejecuta pueden ser distintos y nadie lo nota."
              },
              "integrity": {
                "type": "string",
                "description": "Digest del paquete, cuando el origen lo publica."
              }
            }
          },
          "transport": {
            "type": "string",
            "enum": [
              "stdio",
              "http",
              "sse"
            ]
          },
          "endpoint": {
            "type": "string",
            "description": "Solo para http y sse. Equivale a `remotes[].url` del registro oficial."
          },
          "write_operations": {
            "type": "boolean",
            "description": "Si alguna herramienta MODIFICA algo fuera del cliente. Es el unico indicador de RIESGO que se declara, porque es el que decide quien tiene que aprobar. Deliberadamente NO se pide la lista de herramientas PERMITIDAS ni los alcances de la credencial: el control efectivo lo ejerce la credencial, no este archivo, y un campo que parece un control sin serlo da falsa confianza. Eso NO alcanza a `tools_digest`, que es otra cosa: no autoriza nada, DETECTA que la superficie declarada cambio. Autorizar y detectar son problemas distintos y se resuelven en sitios distintos."
          },
          "tools_digest": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "sha256 de las herramientas que el servidor declaraba CUANDO SE APROBO: nombre, descripcion y esquema de entrada de cada una, en forma canonica. Se registra al aprobar y una comprobacion periodica lo recalcula; si difiere, la superficie cambio sin pasar por revision. QUE ATAQUE CIERRA: el *rug pull* -- CVE-2025-54136, taxonomia MCP-38 --, en el que un servidor de confianza cambia la descripcion de una herramienta despues de ser aprobado. Y la descripcion de una herramienta es una INSTRUCCION PARA EL MODELO, asi que cambiarla es inyeccion de prompt sin tocar el codigo del plugin. El protocolo MCP no ofrece ninguna primitiva contra esto -- sin firma, sin version que el cliente deba fijar, sin notificacion que los clientes honren -- y ningun cliente avisa cuando una definicion cambia. LIMITE: DETECTA, no previene; la ventana es el intervalo de comprobacion. NO entra en el digest nada mas que esos tres campos ni el orden en que el servidor las devuelva, porque un digest que cambia por ruido se vuelve una alarma que se aprende a ignorar."
          },
          "tools_digest_date": {
            "type": "string",
            "format": "date",
            "description": "Cuando se registro el digest. Sin esta fecha no se sabe si la comprobacion periodica compara contra algo reciente o contra algo de hace un año."
          }
        },
        "allOf": [
          {
            "if": {
              "properties": {
                "transport": {
                  "enum": [
                    "http",
                    "sse"
                  ]
                }
              },
              "required": [
                "transport"
              ]
            },
            "then": {
              "required": [
                "endpoint"
              ],
              "description": "Con transporte remoto, DONDE conecta es parte del gobierno: es el destino al que sale el dato. Con stdio no hay endpoint que declarar."
            }
          },
          {
            "if": {
              "properties": {
                "source": {
                  "properties": {
                    "kind": {
                      "const": "remote"
                    }
                  },
                  "required": [
                    "kind"
                  ]
                }
              },
              "required": [
                "source"
              ]
            },
            "then": {
              "required": [
                "tools_digest",
                "tools_digest_date"
              ],
              "description": "UN SERVIDOR REMOTO NO SE APRUEBA SIN EL DIGEST DE SUS HERRAMIENTAS. En los demas origenes el codigo se descarga y `version_pin` lo fija: ese paquete exacto es inmutable, y cambiarlo exige subir la version y pasar por un pull request. Un remoto corre en la infraestructura de otro y NO HAY VERSION QUE FIJAR -- su dueño puede cambiar mañana lo que sus herramientas declaran, y las descripciones son instrucciones para el modelo --. Ahi el digest no es un refuerzo: es el UNICO control que existe, y aprobar un remoto sin el es confianza ciega en un tercero. En los origenes descargables el digest es opcional porque `version_pin` ya cubre lo esencial."
            }
          }
        ]
      }
    },
    "credentials": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "mechanism"
      ],
      "x-read-from": "METADATA.json",
      "description": "APORTACION NETA: el `server.json` del registro oficial declara que una credencial es necesaria y secreta, pero NO su dueno; y Backstage no tiene anotacion de «como se solicita el acceso». Es el unico campo del que no encontre equivalente en ningun origen.",
      "properties": {
        "mechanism": {
          "type": "string",
          "enum": [
            "none",
            "secret-ref",
            "oauth",
            "workload-identity"
          ]
        },
        "refs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Los nombres de las variables de entorno, nunca sus valores. Equivale a `environmentVariables` del registro oficial."
        },
        "ownership": {
          "type": "object",
          "additionalProperties": false,
          "description": "SOLO se exige con `secret-ref`, porque es el unico mecanismo donde alguien tiene que CONCEDER algo a mano: con oauth o workload-identity el desarrollador se autentica con su propia identidad y no hay nada que pedir. El defecto que cierra no es tecnico, es de enrutado: el artefacto viaja sin secreto, el cliente pide el valor, y el desarrollador no sabe a quien pedirlo — el `owner_team` del artefacto es quien lo PUBLICO, no quien lo custodia.",
          "properties": {
            "credential_owner": {
              "type": "string",
              "description": "El EQUIPO que custodia el secreto, nunca una persona: un dueno individual deja la entrada huerfana en cuanto cambia de puesto."
            },
            "access_request_url": {
              "type": "string",
              "description": "DONDE se pide. Saber el equipo no basta: sin la via, el flujo acaba en un correo a ciegas."
            },
            "entitlement": {
              "type": "string"
            },
            "secret_ref": {
              "type": "string"
            }
          },
          "required": [
            "credential_owner",
            "access_request_url"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "mechanism": {
                "const": "secret-ref"
              }
            },
            "required": [
              "mechanism"
            ]
          },
          "then": {
            "required": [
              "ownership"
            ],
            "description": "Con `secret-ref` alguien tiene que CONCEDER el acceso a mano, asi que hay que decir quien y donde se pide. Con oauth o workload-identity el desarrollador usa su propia identidad y no hay nada que pedir."
          }
        }
      ]
    },
    "approval": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "approved_by",
        "date",
        "review_by",
        "security_review"
      ],
      "x-read-from": "METADATA.json",
      "description": "Obligatorio porque un `mcp` es el unico tipo cuyo estado se degrada SIN QUE NADIE TOQUE NADA. `review_by` no es burocracia: es la fecha en que la aprobacion vence y el artefacto vuelve a revision.",
      "properties": {
        "approved_by": {
          "type": "string"
        },
        "date": {
          "type": "string",
          "format": "date"
        },
        "review_by": {
          "type": "string",
          "format": "date"
        },
        "security_review": {
          "type": "boolean"
        },
        "notes": {
          "type": "string"
        }
      }
    }
  },
  "unevaluatedProperties": false
}
